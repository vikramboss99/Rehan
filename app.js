const page=Number(document.body.dataset.page||1);const $=s=>document.querySelector(s);function nav(){let n=$('#nav');if(n)n.innerHTML='<div class="dots">'+Array.from({length:6},(_,i)=>`<span class="dot ${i+1===page?'active':''}"></span>`).join('')+'</div>'}nav();function sound(type='pop'){try{const c=new(window.AudioContext||window.webkitAudioContext)(),o=c.createOscillator(),g=c.createGain();o.connect(g);g.connect(c.destination);o.type=type==='magic'?'sine':'triangle';let t=c.currentTime;o.frequency.setValueAtTime(type==='magic'?880:300,t);o.frequency.exponentialRampToValueAtTime(type==='magic'?440:95,t+.22);g.gain.setValueAtTime(.13,t);g.gain.exponentialRampToValueAtTime(.001,t+.3);o.start(t);o.stop(t+.32)}catch(e){}}function confetti(n=70){for(let i=0;i<n;i++){let s=document.createElement('span');s.className='spark';s.textContent=['✨','🎉','💖','⭐','🌸'][Math.floor(Math.random()*5)];s.style.left=(25+Math.random()*50)+'vw';s.style.top=(20+Math.random()*50)+'vh';s.style.setProperty('--x',(Math.random()*900-450)+'px');s.style.setProperty('--y',(Math.random()*800-400)+'px');s.style.fontSize=(14+Math.random()*22)+'px';document.body.appendChild(s);setTimeout(()=>s.remove(),1300)}}function fireworks(){confetti(110);try{let c=new(window.AudioContext||window.webkitAudioContext)(),buffer=c.createBuffer(1,c.sampleRate*.65,c.sampleRate),data=buffer.getChannelData(0);for(let i=0;i<data.length;i++)data[i]=(Math.random()*2-1)*Math.pow(1-i/data.length,2);let src=c.createBufferSource(),gain=c.createGain(),filter=c.createBiquadFilter();filter.type='lowpass';filter.frequency.value=2800;src.buffer=buffer;src.connect(filter);filter.connect(gain);gain.connect(c.destination);gain.gain.value=.35;src.start();setTimeout(()=>c.close(),1200)}catch(e){}}function next(p){location.href='page'+p+'.html'}
